
//////////////// กำหนดพินเริ่มต้น //////////////////////

void initial_pin()
{
  pinMode(dir485, OUTPUT);    /////////// dir 485
  pinMode(pin_led, OUTPUT);
  pinMode(pin_led_o, OUTPUT);
  pinMode(pin_led_r, OUTPUT);
  pinMode(pin_led_w, OUTPUT);
  pinMode(buttonPin, INPUT);
}
